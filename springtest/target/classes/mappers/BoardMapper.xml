<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.green.springtest.dao.BoardDao">
  <select id="getList" resultType="kr.green.springtest.vo.BoardVo">
    select * from board where isdel like "N" order by num desc limit 0, 10
  </select>
  <select id="getBoard" resultType="kr.green.springtest.vo.BoardVo">
  	select * from board where num = #{num}
  </select>
  <insert id="setBoard">
  	insert into board(title, writer, content)
  	value(#{board.title}, #{board.writer}, #{board.content})
  </insert>
  <update id="updateBoard">
  	update board set title = #{board.title}, content = #{board.content}, isdel = #{board.isdel}, delDate = #{board.delDate}, views= #{board.views} where num=#{board.num}
  </update>
  <update id="modifyBoard">
  	update board set title = #{board.title}, content = #{board.content} where num=#{board.num}
  </update>
  <update id="delBoard">
  	update board set isdel = "Y", delDate = NOW() where num=#{num}
  </update>
</mapper>
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.green.springtest.dao.BoardDao">
  <select id="getList" resultType="kr.green.springtest.vo.BoardVo">
    select * from board where isdel like "N"
    <if test="cri.type == 0">
    	and (title like concat('%',#{cri.search},'%') 
    	or content like concat('%',#{cri.search},'%') 
    	or writer like concat('%',#{cri.search},'%'))
    </if>
    <if test="cri.type == 1">
    	and title like concat('%',#{cri.search},'%')
    </if>
    <if test="cri.type == 2">
    	and content like concat('%',#{cri.search},'%') 
    </if>
    <if test="cri.type == 3">
    	and writer like concat('%',#{cri.search},'%')
    </if>
    order by num desc limit ${cri.pageStart}, ${cri.perPageNum}  
  </select>
  <select id="getBoard" resultType="kr.green.springtest.vo.BoardVo">
  	select * from board where num = #{num}
  </select>
  <insert id="setBoard">
  	insert into board(title, writer, content, file) value(#{board.title}, #{board.writer}, #{board.content}, #{board.file})
  </insert>
  <update id="updateBoard">
  	update board set title = #{board.title}, content = #{board.content}, isdel = #{board.isdel}, delDate = #{board.delDate}, views= #{board.views} where num=#{board.num}
  </update>
  <update id="modifyBoard">
  	update board set title = #{board.title}, content = #{board.content}, file = #{board.file} where num=#{board.num}
  </update>
  <update id="delBoard">
  	update board set isdel = "Y", delDate = NOW() where num=#{num}
  </update>
  <select id="getTotalCount" resultType="int">
    select count(*) from board where isdel like "N"
	<if test="cri.type == 0">
    	and (title like concat('%',#{cri.search},'%') 
    	or content like concat('%',#{cri.search},'%') 
    	or writer like concat('%',#{cri.search},'%'))
    </if>
    <if test="cri.type == 1">
    	and title like concat('%',#{cri.search},'%')
    </if>
    <if test="cri.type == 2">
    	and content like concat('%',#{cri.search},'%') 
    </if>
    <if test="cri.type == 3">
    	and writer like concat('%',#{cri.search},'%')
    </if>
  </select>
  <select id="isLike" resultType="int">
  	select count(*) from spring.like where boNum = #{boNum} and id = #{id}
  </select>
  <insert id="userLike">
	insert into spring.like(id, boNum) value(#{id}, #{boNum})
  </insert>
  <update id="likeUp">
  	update board set board.like = (select count(*) from spring.like where boNum = #{boNum}) where num=#{boNum}
  </update>
</mapper>